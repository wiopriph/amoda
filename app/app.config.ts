export default defineAppConfig({
  ui: {
    colors: {
      primary: 'emerald',
      neutral: 'slate',
    },

    container: {
      base: 'w-full max-w-(--ui-container) mx-auto px-2 sm:px-4 lg:px-6',
    },
  },
});
